<% @bicycles.each do |bicycle| %>
  <%= link_to bicycle.name, bicycle_path(bicycle) %>
  <%= bicycle.desc %>
  <br>
  <%= link_to 'Edit', edit_bicycle_path(bicycle) %>
  <%= link_to 'Delete', bicycle_path(bicycle), method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>